{
  "name": "@align/align",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "dotnet:api": "dotnet run --launch-profile https --project ./apps/api",
    "dotnet:api/test": "dotnet test ./apps/api-tests",
    "docker:up": "docker compose up"
  },
  "devDependencies": {
    "turbo": "latest"
  },
  "packageManager": "pnpm@8.9.0",
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "prettier": "^3.3.3"
  }
}
